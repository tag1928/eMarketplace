spring:
  application:
    name: eMarketplace
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  jpa:
    generate-ddl: false
    show-sql: false
  flyway:
    baseline-on-migrate: true